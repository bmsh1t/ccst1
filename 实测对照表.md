# 「过度约束 AI」主张 vs 实测对照表

**测量日期**：2026-09-11
**仓库**：`/root/tool/ccst(rename-_Ya66f2)/ccst`
**被测主张来源**：`架构分析总结.md` 第一节「理念 vs 实现」表
**复核方式**：全部结论可由 `/tmp/verify_claims.sh` 一条命令复现，无主观评分

---

## 复核命令

```bash
bash /tmp/verify_claims.sh
```

脚本内容见本文末尾「附录 A」。所有 `C*` 编号与下表一一对应。

---

## 第一部分：四行主张逐条实测

### C1 — 主张「信任判断力 5/10：强制证据链条、覆盖矩阵检查限制了判断」

| 测量项 | 命令 | 实测值 |
|---|---|---|
| 状态顺序转换校验 | `rg 'raise.*(promote\|previous status\|must.*lead)' tools/action_queue.py` | **0** |
| coverage gap 强制解释 | `rg 'raise.*(gap\|explain)' tools/coverage_matrix.py` | **0** |

**初筛曾命中 3 处**，逐行核验后全部排除：

| 行号 | 内容 | 性质 |
|---|---|---|
| `action_queue.py:41` | `ACTIVE_STATUSES = {"queued","running","lead","signal","candidate"}` | 集合定义，非校验 |
| `action_queue.py:59` | `"record tested, dead-end, blocked, lead, signal, candidate, or validated "` | argparse help 文本，非校验 |
| `action_queue.py:2029` | `if previous != "running" and normalized in {...}` | `attempts` 计数器，非校验 |

**行为佐证**（本会话真实执行，非构造）：

```
$ python3 -c "import json; q=json.load(open('state/127.0.0.1:3001/action_queue.json')); \
  print([(a['id'],a['status']) for a in q['actions']])"
AQ-0002: validated     ← 从 queued 直达 candidate → validated，无 lead/signal 中间态
AQ-0001: dead-end      ← 从 queued 直达 dead-end
```

若存在"强制 4 级链条"，这两条路径都不可能完成。**实测拦截次数 = 0**。

**判定：不成立。**

---

### C2 — 主张「信任测试方法选择 3/10：强制 Context Pack 召回，即使基础知识也要查包」

| 测量项 | 命令 | 实测值 |
|---|---|---|
| claim 路径引用 context_pack | `rg 'context_pack\|build_context_pack\|pack_ref' tools/action_queue.py` | **0** |
| 规则已含豁免条款 | `rg -c '不触发\|复用\|不重复查包' skills/runtime-protocol.md` | **6** |

**工具自我声明**（`tools/context_pack.py:3097`）：

> "Skill and knowledge-card fields are **advisory**. Claude must explicitly choose and load..."

**规则已内建的三类豁免**（`skills/runtime-protocol.md`）：
1. 纯解释/规划/复盘 **不触发** recall gate
2. 已有匹配 Pack **直接复用**，不重复查包
3. 同一 target/focus/实质证据 **可复用**

**判定：不成立。** 无代码 gate，且规则自身已声明工具输出为 advisory。

---

### C3 — 主张「信任推理能力 6/10：Loop Guard 机制好，但强制三模式切换打断推理」

| 测量项 | 命令 | 实测值 |
|---|---|---|
| `tools/` 中三模式/mode 关键词 | `rg 'Discovery mode\|Exploitation mode\|Validation mode\|current_mode\|declare_mode' tools/*.py` | **0** |

三模式**仅存在于文档** `skills/runtime-protocol.md:78`，无任何代码检查、记录或要求模式声明。

**同文档的反向条款**（`runtime-protocol.md:93`）：

> "AI selection / override 是能力上限保护：当前 Skill **可以跳过建议路线**、组合知识卡、创建新的 action 类型，或**局部重排模式顺序**"

Loop Guard 部分主张**成立**：`tools/autopilot_state.py:3388` 存在 `build_loop_guard_projection`。

**判定：三模式部分不成立；Loop Guard 部分成立。**

---

### C4 — 主张「信任知识储备 2/10：63 张知识卡需要"晋升仪式"才能跨目标复用」

| 测量项 | 命令 | 实测值 |
|---|---|---|
| 卡片总数 | `ls knowledge/cards/*.md \| wc -l` | **58**（主张 63） |
| maturity 分布 | `rg --no-filename '^maturity: draft' knowledge/cards/*.md \| wc -l` | **58 / 58** |
| maturity 是否被代码 gate | `rg 'maturity' tools/context_pack.py tools/knowledge_registry.py` | **0** |
| `knowledge/candidates/` 实际数量 | `ls knowledge/candidates/*.json \| wc -l` | **0** |

**实测反证**：58 张卡**全部是 `maturity: draft`，且代码完全不读取 maturity**。卡片无需晋升即可被选中加载。

实际选择结果（`context_pack.py --focus "order-history object-level authorization IDOR cross-actor"`）：

```
knowledge_cards: ['knowledge/cards/api-idor.md', 'knowledge/cards/auth-access.md']
card_catalog 发布卡片数: 58
```

**"晋升仪式"作用于 `knowledge/candidates/`——该目录为空**（仅 `.gitkeep`），流程从未被触发。
且 `knowledge/promotion-rules.md:130` 原文："**信号可选**，旧 Candidate 不需迁移"。

**判定：不成立，且方向相反。** 知识储备是最不受约束的一项，应为最高分。

---

## 第二部分：六项理念实现状态

| # | 理念 | 代码强制点 | 实测证据 | 判定 |
|---|---|---|---|---|
| 1 | 信任判断力 | **0** | C1：无顺序校验；本会话 0 拦截 | ✅ 已实现 |
| 2 | 信任测试方法选择 | **0** | C2：claim 不引用 pack；规则 6 条豁免；工具自称 advisory | ✅ 已实现 |
| 3 | 信任推理能力 | **0** | C3：三模式 0 代码命中；文档明确允许重排 | ✅ 已实现 |
| 4 | 信任知识储备 | **0** | C4：58/58 draft，无 maturity gate，0 candidates | ✅ 已实现 |
| 5 | 约束危险操作 | — | `red-lines.md` 28 行，被 3 个 command 引用 | ⚠️ 见下 |
| 6 | 明确报告标准 | — | C5：`seven_question_gate` 在 4 个工具中实现 | ⚠️ 见下 |

### 第 5、6 项的实测细节

| 测量项 | 命令 | 实测值 |
|---|---|---|
| `red-lines.md` 被工具代码加载 | `rg 'red-lines' tools/*.py` | **0** |
| `seven_question_gate` 实现位置 | `rg -ln 'seven_question_gate' tools/*.py` | **4 个**（validate / structured_findings / report_generator / remember） |
| 报告门判定 | `rg 'report_ready = bool' tools/validate.py` | **1 处**（`validate.py:600`） |

**说明**：
- `red-lines.md` 仅被 `commands/*.md` 引用（3 处），**不被 Python 代码加载**——是提示词层约束，不是代码层 gate。
- `seven_question_gate` 有真实代码实现（4 个工具），`validate.py:600` 的 `report_ready` 是真实报告门。

**判定**：第 6 项（报告标准）**有代码实现**，成立。第 5 项（红线）是提示词层约束，**无代码 gate**——这与项目自我描述一致（`rules/red-lines.md` 是"动作副作用判断的最高优先级规则"，由 AI 遵守）。

---

## 第三部分：主张未提及的实测结构问题

以下均为本次实测发现，**三份分析文档（`plan/task_plan.md`、`架构分析总结.md`）均未提及**。

| # | 测量项 | 实测值 | 含义 |
|---|---|---|---|
| S1 | `tools/` 总行数 | **87,536** | 执行层规模 |
| S2 | `skills/` 判断层行数 | **1,599**（`skills/*/SKILL.md`） | 判断层规模 |
| S3 | **比例 S1:S2** | **≈ 55 : 1** | 设计意图是"AI 判断、工具执行"，工程量分布相反 |
| S4 | `autopilot_state.py` 单文件 | **7,489 行 / 127 函数** | 混合 5 类职责（状态读取/投影/gate/Loop Guard/CLI） |
| S5 | 循环依赖 `action_queue → checkpoint` | **1**（`action_queue.py:1716`，函数内延迟导入） | 真实循环 |
| S6 | 循环依赖 `checkpoint → action_queue` | **1**（`checkpoint.py:38`，模块级导入） | 真实循环 |

### S4 的职责分布实测

| 职责类型 | 函数数 |
|---|---|
| 决策类（gate/next/frontier/select/rank） | 23 |
| 存储/投影类（load/save/write/project/format） | 41 |
| 其他辅助 | 63 |
| **合计** | **127** |

### S5/S6 的具体形态

```
checkpoint.py:38        from tools.action_queue import (...)        ← 模块级
action_queue.py:1716    from tools.checkpoint import build_checkpoint  ← 函数内（掩盖循环）
```

### 结构问题与本会话真实缺陷的关联

本会话盲测暴露的 `validate ↔ action_queue` 集成缺口（validate 调用 `resolve_action(status="validated")` 不传 metadata，而 `_versioned_terminal_plan` 要求恰好一个 continuation 或 kill）：

- 两侧分别位于 `validate.py`（2,280 行）与 `action_queue.py`（2,338 行）
- **没有任何单一位置声明该契约**
- 结果：缺陷在盲测时才暴露，而非编码时

这是 S1-S6 的直接后果，也是三份文档均未识别的真实瓶颈。

---

## 第四部分：方法论问题

| # | 问题 | 具体表现 |
|---|---|---|
| M1 | **以文档推代码** | 四行主张的"说明"列全部指向 `rules/`、`skills/` 文档，**无一行引用代码**；但"实现状态"应由代码决定 |
| M2 | **分数无来源** | 5/10、3/10、6/10、2/10 无测量方法（非测试通过率、非拦截次数） |
| M3 | **结论与自身数据矛盾** | 称"知识储备 2/10"（最低分）同时称"63 张卡"；实测 58 张全部可用、无需晋升 |
| M4 | **概念混淆（最关键）** | 将"工具代码行数"当作"AI 上下文成本"。实测：`context_pack.py` 3,261 行源码，**AI 从不读它**；AI 只读其输出 **1,439 tokens** |

### M4 的实测数据

| 项目 | 文档声称 | 实测 |
|---|---|---|
| Context Pack 输出 | "3552 行过滤" | **1,439 tokens**（markdown） |
| `autopilot_state --bounded` | — | **356 tokens** |
| `action_queue summary` | — | **45 tokens** |
| `coverage find-gaps` | "每个 gap 都要解释" | **0 字节**（无 gap） |

---

## 第五部分：结论

| 主张 | 实测判定 |
|---|---|
| 信任判断力被限制 | ❌ 不成立（0 强制点，0 实际拦截） |
| 强制 Context Pack 召回 | ❌ 不成立（工具自称 advisory，规则含 6 条豁免） |
| 强制三模式切换 | ❌ 不成立（0 代码命中，文档明确允许重排） |
| 知识卡需要晋升才能复用 | ❌ 不成立且方向相反（58/58 draft，无 gate） |
| 约束危险操作已实现 | ⚠️ 部分（提示词层，非代码层） |
| 明确报告标准已实现 | ✅ 成立（4 个工具实现 + `report_ready` 门） |
| **执行层结构失控** | ✅ **成立，且文档未提及** |

**核心结论**：项目真实瓶颈**不在判断层被过度约束**（实测强制点为 0），而在**执行层结构**——87,536 行 vs 判断层 1,599 行的分布（55:1）、7,489 行的单文件、两处真实循环依赖、以及跨 2,000+ 行文件的隐式契约。

---

## 附录 A：可复核脚本

保存为 `/tmp/verify_claims.sh`，`bash /tmp/verify_claims.sh` 直接复现本文全部 `C*` 数据。

```bash
#!/usr/bin/env bash
cd -- "/root/tool/ccst(rename-_Ya66f2)/ccst" || exit 1
p(){ printf "  %-34s %s\n" "$1" "$2"; }
n(){ rg -c "$1" $3 2>/dev/null | awk '{s+=$1} END {print s+0}'; }

echo "══ C1 证据链顺序强制 ══"
p "顺序转换校验(must promote to)" "$(n 'raise.*(promote|previous status|must.*lead)' '' tools/action_queue.py)"
p "coverage gap 强制解释"          "$(n 'raise.*(gap|explain)' '' tools/coverage_matrix.py)"

echo "══ C2 Context Pack 强制 ══"
p "claim 路径引用 context_pack"    "$(n 'context_pack|build_context_pack|pack_ref' '' tools/action_queue.py)"
p "规则已含豁免条款"               "$(n '不触发|复用|不重复查包' '' skills/runtime-protocol.md)"

echo "══ C3 三模式代码 gate ══"
p "tools/ 三模式/mode 关键词"      "$(n 'Discovery mode|Exploitation mode|Validation mode|current_mode|declare_mode' '' 'tools/*.py')"

echo "══ C4 知识卡晋升门槛 ══"
p "卡片总数"                       "$(ls knowledge/cards/*.md | wc -l)"
p "maturity=draft"                 "$(rg --no-filename '^maturity: draft' knowledge/cards/*.md | wc -l)"
p "maturity 被代码 gate"           "$(n 'maturity' '' tools/context_pack.py tools/knowledge_registry.py)"
p "candidates 实际数量"            "$(ls knowledge/candidates/*.json 2>/dev/null | wc -l)"

echo "══ C5 约束项真实强制力 ══"
p "red-lines.md 被工具加载"        "$(n 'red-lines' '' tools/*.py)"
p "seven_question_gate 实现"       "$(rg -ln 'seven_question_gate' tools/*.py | wc -l)"
p "报告门 report_ready 判定"       "$(n 'report_ready = bool' '' tools/validate.py)"

echo "══ C6 结构（对照组）══"
p "tools/ 行数"                    "$(fd -e py . tools/ -d 1 2>/dev/null | xargs cat | wc -l)"
p "skills/ 行数"                   "$(cat skills/*/SKILL.md 2>/dev/null | wc -l)"
p "autopilot_state.py 行数"        "$(wc -l < tools/autopilot_state.py)"
p "循环依赖 aq→ck / ck→aq"         "$(n 'from tools.checkpoint import build_checkpoint' '' tools/action_queue.py) / $(n 'from tools.action_queue import' '' tools/checkpoint.py)"
```

## 附录 B：本文数据的实测输出

```
══ C1 证据链顺序强制 ══
  顺序转换校验(must promote to) 0
  coverage gap 强制解释          0
══ C2 Context Pack 强制 ══
  claim 路径引用 context_pack    0
  规则已含豁免条款           6
══ C3 三模式代码 gate ══
  tools/ 三模式/mode 关键词    0
══ C4 知识卡晋升门槛 ══
  卡片总数                       58
  maturity=draft                     58
  maturity 被代码 gate            0
  candidates 实际数量            0
══ C5 约束项真实强制力 ══
  red-lines.md 被工具加载       0
  seven_question_gate 实现         4
  报告门 report_ready 判定      1
══ C6 结构（对照组）══
  tools/ 行数                      87536
  skills/ 行数                     1599
  autopilot_state.py 行数          7489
  循环依赖 aq→ck / ck→aq     1 / 1
```

## 附录 C：口径说明

| 项 | 口径 |
|---|---|
| `skills/` 行数 | 12 个 `skills/*/SKILL.md`（1,599 行）。不含 `skills/README.md`(230) 与 `skills/runtime-protocol.md`（后者是常驻契约） |
| `tools/` 行数 | `tools/` 顶层全部 `.py`（不含子目录） |
| 强制点 | 代码中会 `raise` 或阻断执行、且针对 AI 判断内容（非参数格式校验）的位置 |
| 拦截次数 | 本会话中 owner 因约束拒绝请求的实际次数 |

**未验证项**（本文不主张）：
- 卡片"6-9 KB"大小区间（未精确测量分布）
- "50 个 untested 单元格"（当前 `find-gaps` 为空，无法验证历史状态）
- 三份文档的一致性问题（各文档独立评估，未做交叉核对）
