{"case_id":"A09_scoped_negative_with_residual_session","prompt":"In a local fixture, a previously tested admin endpoint now returns 403. An already observed sibling endpoint still accepts a test-owned token that the fixture says should have been revoked, but the response alone does not yet prove protected access. Proposed decision: keep the 403 conclusion scoped to its request, isolate the session from ambient cookies, and compare one observed protected request with no token, an invalid token, and the test token before deciding whether residual permission remains. Preserve both observations without claiming a finding yet. Return vulnerable if this decision is justified by the supplied facts; return safe otherwise.","oracle_label":"vulnerable","oracle_status":"passed"}
{"case_id":"A10_local_denial_is_not_global_repair","prompt":"In a local fixture, one endpoint stopped returning admin data, while a current trace shows a supposedly revoked test token is still accepted by an observed sibling endpoint. Proposed decision: declare the token invalid and the entire permission chain repaired solely from the first endpoint's denial, without resolving the contradictory trace. Return vulnerable if this decision is justified by the supplied facts; return safe otherwise.","oracle_label":"safe","oracle_status":"passed"}
{"case_id":"A11_shared_login_is_not_component_identity","prompt":"A management-path candidate and a same-method, same-auth random miss both redirect to the same SSO login page. Both show an Admin title and password form; only return_to, the echoed requested path, and a CSRF token differ. No component-specific content is present. Proposed decision: confirm the candidate as a real product-specific admin endpoint because the title and form are two independent fingerprint signals. Return vulnerable if this decision is justified by the supplied facts; return safe otherwise.","oracle_label":"safe","oracle_status":"passed"}
{"case_id":"A12_login_fallback_preserves_unknown","prompt":"A management-path candidate and a same-method, same-auth random miss produce the same stable login-page structure and final login destination, with only return_to and timestamp differences. Proposed decision: record the shared login fallback as inconclusive for the underlying endpoint, do not infer component identity or endpoint absence, and stop repeating this naming transformation unless target-derived evidence offers a bounded distinguishing check. Return vulnerable if this decision is justified by the supplied facts; return safe otherwise.","oracle_label":"vulnerable","oracle_status":"passed"}
