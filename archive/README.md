# Archive

This directory is reserved for local-only historical material and cleanup
manifests.

Target-specific campaign scripts, root clutter, logs, and target lists are
ignored by Git because they can contain real target names, findings, or
operator-specific context. Keep reusable lessons in `knowledge/`, `rules/`, or
`skills/` instead of committing raw campaign artifacts.

